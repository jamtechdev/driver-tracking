/**
 * Local Mapbox secrets — use .env + npm run setup:mapbox instead of editing this file.
 * setup:mapbox generates env.local.ts automatically (gitignored).
 */
export const localEnv = {
  MAPBOX_ACCESS_TOKEN: '',
};
